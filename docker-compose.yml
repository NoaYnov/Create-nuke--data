version: '3.8'

# ============================================
# Create Nuclear Stats - Docker Compose
# ============================================
# Services:
#   - postgres: Base de données PostgreSQL 15
#   - streamlit-app: Application principale (port 8501)
#   - streamlit-onepage: Vue simplifiée (port 8502)
#   - stats-collector: Collecteur de statistiques
# ============================================

services:
  # ==========================================
  # PostgreSQL Database
  # ==========================================
  postgres:
    image: postgres:15-alpine
    container_name: createnuclear-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-createnuclear_stats}
      POSTGRES_USER: ${POSTGRES_USER:-admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
      # Configuration PostgreSQL
      POSTGRES_HOST_AUTH_METHOD: scram-sha-256
    volumes:
      # Volume persistant pour les données
      - pgdata:/var/lib/postgresql/data
      # Scripts d'initialisation (exécutés au premier démarrage)
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-admin} -d ${POSTGRES_DB:-createnuclear_stats}" ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 60s
    restart: unless-stopped
    networks:
      - createnuclear-network
    # Limites de ressources (ajustez selon vos besoins)
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M
    # Configuration PostgreSQL supplémentaire
    command:
      - "postgres"
      - "-c"
      - "max_connections=100"
      - "-c"
      - "shared_buffers=256MB"
      - "-c"
      - "effective_cache_size=1GB"
      - "-c"
      - "maintenance_work_mem=64MB"
      - "-c"
      - "checkpoint_completion_target=0.9"
      - "-c"
      - "wal_buffers=16MB"
      - "-c"
      - "default_statistics_target=100"
      - "-c"
      - "random_page_cost=1.1"
      - "-c"
      - "effective_io_concurrency=200"
      - "-c"
      - "work_mem=2621kB"
      - "-c"
      - "min_wal_size=1GB"
      - "-c"
      - "max_wal_size=4GB"
      - "-c"
      - "log_timezone=UTC"
      - "-c"
      - "timezone=UTC"

  # ==========================================
  # pgAdmin - Database Management UI
  # ==========================================
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: createnuclear-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@createnuclear.local}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
      PGADMIN_CONFIG_SERVER_MODE: 'False'
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: 'False'
    ports:
      - "${PGADMIN_PORT:-5050}:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - createnuclear-network
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M

  # ==========================================
  # Streamlit Application Principale
  # ==========================================
  streamlit-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: createnuclear-app
    command:
      - "python"
      - "-m"
      - "streamlit"
      - "run"
      - "src/ui/streamlit_app.py"
      - "--server.port=8501"
      - "--server.address=0.0.0.0"
      - "--server.headless=true"
      - "--browser.gatherUsageStats=false"
    ports:
      - "${STREAMLIT_PORT:-8501}:8501"
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER:-admin}:${POSTGRES_PASSWORD_ENCODED}@${POSTGRES_HOST:-postgres}:5432/${POSTGRES_DB:-createnuclear_stats}
      - CURSEFORGE_API_KEY=${CURSEFORGE_API_KEY}
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
      - TZ=UTC
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - createnuclear-network
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8501/_stcore/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M

  # ==========================================
  # Streamlit One-Page View
  # ==========================================
  streamlit-onepage:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: createnuclear-onepage
    command:
      - "python"
      - "-m"
      - "streamlit"
      - "run"
      - "src/ui/app_onepage.py"
      - "--server.port=8502"
      - "--server.address=0.0.0.0"
      - "--server.headless=true"
      - "--browser.gatherUsageStats=false"
    ports:
      - "${STREAMLIT_ONEPAGE_PORT:-8502}:8502"
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER:-admin}:${POSTGRES_PASSWORD_ENCODED}@${POSTGRES_HOST:-postgres}:5432/${POSTGRES_DB:-createnuclear_stats}
      - CURSEFORGE_API_KEY=${CURSEFORGE_API_KEY}
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
      - TZ=UTC
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - createnuclear-network
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8502/_stcore/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M

  # ==========================================
  # Stats Collector Daemon
  # ==========================================
  stats-collector:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: createnuclear-collector
    command: [ "python", "src/collectors/collect_daemon.py" ]
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER:-admin}:${POSTGRES_PASSWORD_ENCODED}@${POSTGRES_HOST:-postgres}:5432/${POSTGRES_DB:-createnuclear_stats}
      - CURSEFORGE_API_KEY=${CURSEFORGE_API_KEY}
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
      - TZ=UTC
      # Configuration du collecteur
      - COLLECTION_INTERVAL=${COLLECTION_INTERVAL:-86400}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - createnuclear-network
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 256M

# ==========================================
# Volumes
# ==========================================
volumes:
  pgdata:
    driver: local
    name: createnuclear_pgdata
  pgadmin_data:
    driver: local
    name: createnuclear_pgadmin

# ==========================================
# Networks
# ==========================================
networks:
  createnuclear-network:
    driver: bridge
    name: createnuclear_network
